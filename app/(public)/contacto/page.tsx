import { ContactClient } from './contact-client';

export const metadata = {
  title: 'Contacto',
  description: 'Contáctanos para una consultoría gratuita sobre cómo la IA puede multiplicar las ventas de tu negocio.',
};

export default function ContactoPage() {
  return <ContactClient />;
}
